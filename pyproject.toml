[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "gamecrafter"
version = "1.0.0"
description = "Evidence-aware game knowledge and marketing workspace."
readme = "README.md"
requires-python = ">=3.12"
license = { file = "LICENSE" }
authors = [{ name = "Wenqi77Zhang" }]
dependencies = [
    "alembic>=1.18,<2",
    "fastapi>=0.115,<1",
    "httpx2>=2.7,<3",
    "playwright>=1.61,<2",
    "psycopg[binary]>=3.2,<4",
    "pydantic-settings>=2.7,<3",
    "sqlalchemy>=2.0,<3",
    "uvicorn[standard]>=0.34,<1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3,<9",
    "ruff>=0.11,<1",
]

[tool.hatch.build.targets.wheel]
packages = ["src/gamecrafter"]

[tool.pytest.ini_options]
addopts = "-q --strict-markers -p no:cacheprovider --basetemp=.pytest-local"
testpaths = ["tests"]
pythonpath = ["src"]
markers = [
    "postgres: requires a running PostgreSQL test database",
]

[tool.ruff]
target-version = "py312"
line-length = 100
src = ["src", "tests", "apps/api"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "lf"
